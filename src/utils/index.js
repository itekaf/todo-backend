import auth from './auth';
import answer from './answer';
import convert from './convert';
import validate from './validate';

const Utils = {
    auth,
    answer,
    convert,
    validate,
};

export default Utils;
export {
    auth, answer, validate, convert,
};
